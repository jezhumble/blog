source "https://rubygems.org"

# Pins Jekyll and plugins to the versions GitHub Pages builds with,
# so `bundle exec jekyll serve` matches what gets published.
gem "github-pages", group: :jekyll_plugins

# Standard-library gems no longer bundled with Ruby >= 3.4
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
